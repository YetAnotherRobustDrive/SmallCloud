spring:
  servlet:
    multipart:
      max-file-size: 100GB
      max-request-size: 100GB
  profiles:
    default: local
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        globally_quoted_identifiers: true
