version: '3'

services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: pw
      MYSQL_DATABASE: test
      MYSQL_USER: myuser
      MYSQL_PASSWORD: mypassword
    ports:
      - "3333:3306"
    volumes:
      - ./data:/var/lib/mysql

