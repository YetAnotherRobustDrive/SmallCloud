version: '3.8'

services:
  db:
    image: mariadb:latest
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: pw
      MARIADB_DATABASE: test_db
      MARIADB_USER: myuser
      MARIADB_PASSWORD: mypassword
    ports:
      - "3333:3306"
    volumes:
      - ./data:/var/lib/mysql

